<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.main">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_heart_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_heart_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.Main">
        <activity android:name=".caloriesMain"></activity>
        <activity android:name=".todoMain" />
        <activity android:name=".result_Bmi" />
        <activity android:name=".bmiMain" />
        <activity android:name=".stopMain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calender" />
    </application>

</manifest>